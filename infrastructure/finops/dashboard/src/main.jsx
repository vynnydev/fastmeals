import React from "react";
import ReactDOM from "react-dom/client";
import FinOpsDashboard from "./finops-dashboard";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FinOpsDashboard />
  </React.StrictMode>
);
